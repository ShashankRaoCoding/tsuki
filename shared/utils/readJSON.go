package utils 

import (
	"fmt" 
	"os"
	"io"
	"encoding/json" 
)

func ReadJSONToStruct(filePath, structType) (any, err ) {
	var file os.File
	var err error 

	file, err = os.Open(filePath) 
	if err 
}










































