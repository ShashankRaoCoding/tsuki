└── main()
    └── app.New()
        ├── app.Init()
        │   └── appsConfig.LoadApps()
        │       └── utils.ReadDirToStructs()
        │           └── utils.ReadJSONToStruct()
        ├── app.Update()
        │   └── f()
        │       └── msgs.ErrFunc()
        └── app.View()
            └── a.Render()
                ├── a.RenderTabs()
                └── tab.Tab.View()
